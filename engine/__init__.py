"""Pixel Lyrics Engine — terminal TUI variant.

Architecture follows PRD.md (registries, schema, sync engine, clock source)
but renders to a terminal TUI via `rich` (per DESIGN.md for the Merra/Colorcode hook).
"""
